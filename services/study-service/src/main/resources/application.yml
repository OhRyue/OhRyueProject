server:
  port: 8082

spring:
  application:
    name: study-service
  jpa:
    open-in-view: false
  flyway:
    enabled: true

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"